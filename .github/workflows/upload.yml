name: Upate E621Bot
on:
  push:
    branches: ["master"]


jobs:
  build:
    name: Build
    runs-on: self-hosted
    steps:
      - name: Execute remote SSH commands using password
        uses: appleboy/ssh-action@v1
        with:
          script: echo "Hello, World"